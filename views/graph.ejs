<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="css/graph.css">
        <%- include("partials/style") %>
    </head>
    <body>
    
        <div class="<%=graph %>">
            <div class="legend">
                <h3>Legend</h3>
                <hr>
                <ul>
                    <% points.forEach((point) => { %>
                        <li><%= point.name %>: <%= point.color %></li>
                    <% }); %>
                </ul>
            </div>
            <div class="graph">
                <% points.forEach((point) => { %>
                    <div 
                        data-value="<%= point.value %>"
                        class="<%= point.color %>"
                        style="height: calc(100% * (<%= point.value %> / <%= scale %>)); width: 50px;"
                    ></div>
                <% }); %>
            </div>
        </div>
    
    </body>
    </html>